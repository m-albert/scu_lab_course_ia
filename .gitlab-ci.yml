image: ghcr.io/prefix-dev/pixi:0.70.2

stages:
  - deploy

pages:
  stage: deploy
  script: 
  - pixi run -e docs book-clean
  - pixi run -e docs book
  - mkdir public
  - mv ./_build/html/* public/
  - rm -r ./_build
  artifacts:
    paths:
      - public
